# 微信接入与上线指南

> 说明：微信平台规则会调整，具体以对应平台后台（mp.weixin.qq.com / work.weixin.qq.com）页面为准。本指南覆盖微信公众号、企业微信（原企业号）与小程序三条接入路线，供不同主体类型（个人 / 个体工商户 / 企业）对照使用。

## 1. 账号类型总览

| 账号类型 | 主体要求 | 认证 | 网页授权登录 | 支付 | 适用场景 |
| --- | --- | --- | --- | --- | --- |
| 个人订阅号 | 个人 | 不支持 | 不支持 | 不支持 | 内容发布，不能用于本系统登录 |
| 服务号 | 企业 / 个体工商户 | 300 元/年 | 认证后可开 | 可开通商户号 | 本系统默认支持，推荐 |
| 企业微信（原企业号） | 企业 | 免费 | 自建应用 OAuth | 需企业支付 | 已有企业主体 / 企业内使用 |
| 小程序 | 企业 / 个体工商户 | 300 元/年 | web-view 内嵌网页 | 可开通 | 与网页版配合使用 |

说明：

- 个人主体无法注册服务号或企业微信。个人想使用服务号，需先办理个体工商户营业执照。
- 企业微信为免费注册，但需企业主体资料（营业执照、法人信息）。
- 本系统内置的「微信一键登录」实现针对服务号网页授权；企业微信与小程序为可选扩展，见下文对应路线。

## 2. 公共前置步骤（所有路线都需要）

1. 准备域名：在云服务商（阿里云 / 腾讯云等）购买，并完成域名实名认证。
2. 准备服务器：云服务器或轻量应用服务器均可，Windows 或 Linux。
3. 域名 ICP 备案：在大陆使用的域名必须备案，审核约 2~3 周；备案完成前域名不能对外提供访问。**这一项耗时最长，建议最先启动。**
4. 部署本项目并配置 HTTPS（云厂商免费证书或 Let's Encrypt）。
5. 将 `app/.env` 中的 `APP_BASE_URL` 改为正式 HTTPS 地址。

> 本地开发调试可使用内网穿透（ngrok / frp / 花生壳）临时获得公网 HTTPS 地址，但正式上线必须使用备案域名。

## 3. 路线 A：微信服务号（系统原生支持）

适用：个体工商户 / 企业主体，需要顾客在微信内打开网页并一键登录。

### 3.1 注册与认证

1. 前往 mp.weixin.qq.com →「立即注册」→ 选择「服务号」。
2. 主体类型选择「个体工商户」或「企业」，按提示填写统一社会信用代码、执照信息。
3. 验证邮箱、绑定管理员微信（需实名微信扫码确认）。
4. 完成「微信认证」：费用 300 元/年，审核一般 1~5 个工作日。
5. 认证通过后，在「设置与开发 → 基本配置」查看：
   - AppID：公开信息。
   - AppSecret：仅完整显示一次，务必立即保存，禁止提交到代码或 Git。

### 3.2 域名与网页授权配置

1. 在微信后台「设置与开发 → 公众号设置 → 功能设置」中配置「网页授权域名」。
   - 必须为已备案的 HTTPS 域名，不能使用 IP 或 localhost。
   - 按提示将微信提供的验证文件上传到域名根目录完成校验。
2. 在「基本配置」中配置 IP 白名单（调用接口的服务器公网 IP），否则接口调用会报错。

### 3.3 系统配置（app/.env）

```env
WECHAT_APPID=服务号AppID
WECHAT_SECRET=服务号AppSecret
WECHAT_SCOPE=snsapi_base
APP_BASE_URL=https://你的域名
```

- `WECHAT_SCOPE`：
  - `snsapi_base`：静默授权，仅获取 openid（登录足够，推荐）。
  - `snsapi_userinfo`：需要用户点击同意，额外获取昵称、头像。
- 未配置时，系统自动隐藏微信登录入口，账号密码登录不受影响。

### 3.4 验证

- 在微信内打开 `APP_BASE_URL`，登录页应显示微信登录按钮。
- 授权后系统通过回调获取 openid 并建立顾客会话。
- 普通浏览器打开网页时无 openid，微信登录按钮不可用，使用账号密码登录兜底。

## 4. 路线 B：企业微信（原企业号）

适用：已注册企业微信的企业，或需要面向企业员工 / 客户提供服务的主体。

### 4.1 注册与创建应用

1. 前往 work.weixin.qq.com 注册企业微信，完成企业主体认证。
2. 在管理后台「应用管理 → 自建」创建自建应用，获得：
   - CorpID（企业 ID）
   - AgentId（应用 ID）
   - Secret（应用密钥）
3. 在自建应用详情中配置「网页授权及 JS-SDK」可信域名（需备案 HTTPS 域名）。

### 4.2 与本系统的关系

- 企业微信的网页授权接口（CorpID + AgentId + Secret，走 `qyapi.weixin.qq.com`）与服务号网页授权（AppID + Secret，走 `open.weixin.qq.com`）不兼容。
- 本系统当前代码内置的是服务号网页授权。接入企业微信需要新增适配代码（OAuth 入口、回调、令牌缓存），可作为后续版本。
- 若只需要「顾客在微信里找客服」，可先使用企业微信的「微信客服」功能，不修改系统代码。

### 4.3 注意事项

- 企业微信 OAuth 的 `redirect_uri` 同样要求已备案 HTTPS 域名。
- 企业微信应用的 Secret 仅显示一次，需妥善保存。

## 5. 路线 C：小程序（可选）

适用：已有或计划注册小程序，希望用户在微信小程序内直接使用网页系统。

1. 以企业 / 个体工商户主体注册小程序（认证 300 元/年）。
2. 在小程序后台「开发管理 → 开发设置」获取 AppID 与 AppSecret。
3. 在「开发管理 → 开发设置 → 业务域名」中配置本系统域名（需 HTTPS，并上传校验文件）。
4. 在小程序内使用 web-view 组件加载本系统网页；登录绑定走系统的小程序接口。
5. 系统配置：

```env
WECHAT_MINI_APPID=小程序AppID
WECHAT_MINI_SECRET=小程序AppSecret
MINI_BIND_SECRET=绑定令牌签名密钥（可复用 CUSTOMER_SESSION_SECRET）
```

## 6. 常见问题

| 问题 | 说明 |
| --- | --- |
| 网页授权域名配置失败 | 域名未备案，或验证文件未放到域名根目录；需先完成 ICP 备案 |
| 接口返回 401 / 48001 | AppSecret 错误，或服务器 IP 不在白名单 |
| 微信登录按钮不显示 | WECHAT_APPID / WECHAT_SECRET 未配置，或不在微信内置浏览器打开 |
| 普通浏览器打不开微信登录 | 网页授权仅在微信内置浏览器有效，系统以账号密码登录兜底 |
| 个人订阅号能否接入 | 不能：无认证、无网页授权、无支付 |
| 已有企业号能否直接用服务号配置 | 不能：企业微信走 CorpID/AgentId/Secret，需要代码适配（见路线 B） |
| AppSecret 泄露了怎么办 | 在微信后台重置 AppSecret，并同步更新服务器环境变量 |

## 7. 上线前检查

- [ ] 域名已备案，HTTPS 证书有效
- [ ] 微信后台：网页授权域名 / 可信域名已配置并校验通过
- [ ] IP 白名单已加入服务器公网 IP
- [ ] AppSecret 已存入服务器环境变量，未进入 Git
- [ ] APP_BASE_URL 为正式 HTTPS 地址
- [ ] 生产环境已设置强管理密码与随机会话密钥
- [ ] 数据库已备份
