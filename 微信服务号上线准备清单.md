# 微信服务号上线准备清单（个体工商户路线）

> 适用：个人 + 现有账号密码网页系统，想升级为「微信服务号内一键登录 + 微信内使用」
> 更新时间：2026-08-20（微信规则可能调整，以 mp.weixin.qq.com 官方页面为准）

## 总流程

办个体户执照 → 注册服务号 → 微信认证（300 元/年）→ 买域名和服务器 → ICP 备案 → 配置 HTTPS → 微信后台配「网页授权域名」→ 把 AppID/AppSecret/域名发给我 → 我改代码 → 上线。

## 阶段 0：办个体工商户营业执照（你现在就能开始）

1. 准备材料：本人身份证、手机号、一个邮箱。
2. 办理渠道（二选一）：
   - 线上：本地政务 App / 政务服务网（如「粤省事」「浙里办」等，各地区不同），搜索「个体工商户设立登记」；
   - 线下：去当地市场监督管理局（工商所）窗口，带身份证现场办。
3. 免费或仅少量代办费，一般几天到两周内能拿到执照。
4. 经营范围参考写法（以当地窗口核定为准）：「网络技术服务、信息技术咨询服务、互联网信息服务」等。
5. 拿到执照后：拍一张清晰的营业执照照片备用；记住统一社会信用代码。

## 阶段 1：注册微信服务号 + 认证

1. 打开 mp.weixin.qq.com → 右上角「立即注册」→ 选择「服务号」。
2. 主体类型选择「个体工商户」，填写统一社会信用代码、执照信息。
3. 验证邮箱、绑定管理员微信（需要实名微信，扫码确认）。
4. 做「微信认证」：费用 300 元/年，上传执照、法人身份证等信息，审核一般 1~5 个工作日。
5. 认证通过后，到「设置与开发 → 基本配置」查看：
   - AppID（公开，没关系）；
   - AppSecret（**只完整显示一次，务必立刻保存**；泄露就等于别人能用你的公众号登录，绝不能发给无关的人，也绝不能提交到 git）。

> 注意：你现在这个「个人公众号」不能升级成服务号，服务号必须用个体户/企业主体重新注册。个人号可以留着当内容号，不冲突。

## 阶段 2：域名 + 服务器 + HTTPS + ICP 备案

1. 买域名：阿里云 / 腾讯云等注册商，.com/.cn 一年几十元。域名实名认证需要身份证。
2. 买云服务器：轻量应用服务器即可，一年约一两百元起（阿里云、腾讯云都有）。
3. ICP 备案：在购买域名的云厂商后台免费办理，**需要 2~3 周**，备案通过前域名不能对外提供服务。这是最花时间的一步，越早启动越好。
4. 服务器上安装运行环境并部署本项目（部署脚本我可以帮你写好；Windows 服务器或 Linux 都行）。
5. 配置 HTTPS：用云厂商免费证书或 Let's Encrypt 免费证书。
6. 微信服务号后台 →「设置与开发 → 公众号设置 → 功能设置」→「网页授权域名」填你的域名（需要把微信给的验证文件放到域名根目录，认证后可以配置）。

## 阶段 3：把这些信息发给我

- 服务号 AppID 和 AppSecret；
- 正式域名（必须是 HTTPS，例如 `https://deltaclub.example.com`）；
- 服务器登录方式（IP + 账号密码/密钥），或者你只想让我出部署教程也可以；
- 营业执照上的主体名称；
- 是否需要同时做小程序（如果以后想上小程序，同一执照再注册一个小程序即可，web-view 才能用）。

## 我这边会做的代码改造（等你的信息到位）

1. 用户端「微信一键登录」（网页授权 snsapi_userinfo → 拿 openid，绑定为顾客身份）；
2. 打手独立端加「申请入驻」+「微信一键注册/登录」，审核通过后可用微信直接登录；
3. 所有微信配置做成环境变量：不配置时，现在的账号密码功能完全照常可用，不影响本地开发；
4. 部署文档 + 一键启动脚本。

## 常见坑（提前知道，少走弯路）

- 个人订阅号：不能认证、不能网页授权、菜单不能放外链、不能支付——所以做不了这套系统需要的事。
- 网页授权域名：必须是**已备案的 HTTPS 域名**，不能用 IP，不能用 localhost。
- 「微信一键登录」只在**微信内置浏览器打开的网页**里有效；普通浏览器打开网页拿不到 openid，所以系统必须保留账号密码登录作为兜底。
- AppSecret 只放服务器环境变量，不写进代码、不进 git。
- 微信支付属于后续阶段（需要单独开通商户号），第一批先做「微信登录 + 点单」，支付可先用线下收款过渡。
